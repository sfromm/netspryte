#!/usr/bin/python
__requires__ = ['netspryte']
try:
    import pkg_resources
except Exception:
    pass
import sys

from netspryte.commands.rrdtune import RrdTuneCommand

if __name__ == '__main__':
    cmd = RrdTuneCommand()
    sys.exit(cmd.execute())
